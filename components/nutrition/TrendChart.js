// Moved to components/TrendChart.js (2026-08-09) — the member lift-progress
// chart uses it too now, so it's no longer nutrition-specific. This
// re-export keeps the original import path working.
export { TrendChart } from "../TrendChart";
